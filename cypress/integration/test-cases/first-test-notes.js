// SYNTAX:

describe('a describe block', function () {

    it('the it block inside the describe block', function () {

    })
})


//alternatively using the arrow function

describe('a describe block', () => {

    it('the it block inside the describe block', () => {

    })
})

//another alternative using context instead of describe (of course with arrow function or normal function)
context('a describe block', () => {

    it('the it block inside the describe block', () => {

    })
})

